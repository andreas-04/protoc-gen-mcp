// Code generated by protoc-gen-mcp. DO NOT EDIT.
// source: sample/v1/sample.proto

package samplev1

import (
	"context"
	"encoding/json"
	"fmt"

	"github.com/modelcontextprotocol/go-sdk/mcp"
	"google.golang.org/grpc"
	"google.golang.org/protobuf/encoding/protojson"
)


// EchoInput is the MCP tool input type for the Echo RPC.
// Fields mirror the EchoRequest proto message with JSON tags compatible
// with protojson, so the struct can be round-tripped through json.Marshal /
// protojson.Unmarshal.
type EchoInput struct {
	Message string `json:"message" jsonschema:"message is the text to echo."`
	Count int32 `json:"count"`
	Tags []string `json:"tags,omitempty"`
	Optional *string `json:"optional,omitempty"`
	Metadata json.RawMessage `json:"metadata,omitempty"`
}

// ReverseInput is the MCP tool input type for the Reverse RPC.
// Fields mirror the ReverseRequest proto message with JSON tags compatible
// with protojson, so the struct can be round-tripped through json.Marshal /
// protojson.Unmarshal.
type ReverseInput struct {
	Text string `json:"text"`
}

// RegisterEchoServiceTools registers an MCP tool for every unary RPC method
// in EchoService.  Streaming RPCs are intentionally omitted.
//
// Parameters:
//   - s      – the MCP server to register tools on.
//   - client – a connected gRPC client for the EchoService service.
func RegisterEchoServiceTools(s *mcp.Server, client EchoServiceClient) {
	mcp.AddTool(s,
		&mcp.Tool{
			Name:        "echo_service_echo",
			Description: "Echo returns its input verbatim.",
		},
		func(ctx context.Context, _ *mcp.CallToolRequest, in EchoInput) (*mcp.CallToolResult, any, error) {
			// 1. Marshal the typed input struct to JSON.
			inputJSON, err := json.Marshal(in)
			if err != nil {
				return nil, nil, fmt.Errorf("echo_service_echo: marshal input: %w", err)
			}

			// 2. Unmarshal JSON into the proto request message.
			req := new(EchoRequest)
			if err := (protojson.UnmarshalOptions{DiscardUnknown: true}).Unmarshal(inputJSON, req); err != nil {
				return nil, nil, fmt.Errorf("echo_service_echo: build proto request: %w", err)
			}

			// 3. Call the gRPC method.
			resp, err := client.Echo(ctx, req)
			if err != nil {
				return nil, nil, fmt.Errorf("echo_service_echo: gRPC call: %w", err)
			}

			// 4. Marshal the proto response to JSON for the tool result.
			respJSON, err := (protojson.MarshalOptions{UseProtoNames: true, EmitUnpopulated: true}).Marshal(resp)
			if err != nil {
				return nil, nil, fmt.Errorf("echo_service_echo: marshal response: %w", err)
			}

			return &mcp.CallToolResult{
				Content: []mcp.Content{&mcp.TextContent{Text: string(respJSON)}},
			}, nil, nil
		},
	)

	mcp.AddTool(s,
		&mcp.Tool{
			Name:        "echo_service_reverse",
			Description: "Reverse returns its input with characters reversed.",
		},
		func(ctx context.Context, _ *mcp.CallToolRequest, in ReverseInput) (*mcp.CallToolResult, any, error) {
			// 1. Marshal the typed input struct to JSON.
			inputJSON, err := json.Marshal(in)
			if err != nil {
				return nil, nil, fmt.Errorf("echo_service_reverse: marshal input: %w", err)
			}

			// 2. Unmarshal JSON into the proto request message.
			req := new(ReverseRequest)
			if err := (protojson.UnmarshalOptions{DiscardUnknown: true}).Unmarshal(inputJSON, req); err != nil {
				return nil, nil, fmt.Errorf("echo_service_reverse: build proto request: %w", err)
			}

			// 3. Call the gRPC method.
			resp, err := client.Reverse(ctx, req)
			if err != nil {
				return nil, nil, fmt.Errorf("echo_service_reverse: gRPC call: %w", err)
			}

			// 4. Marshal the proto response to JSON for the tool result.
			respJSON, err := (protojson.MarshalOptions{UseProtoNames: true, EmitUnpopulated: true}).Marshal(resp)
			if err != nil {
				return nil, nil, fmt.Errorf("echo_service_reverse: marshal response: %w", err)
			}

			return &mcp.CallToolResult{
				Content: []mcp.Content{&mcp.TextContent{Text: string(respJSON)}},
			}, nil, nil
		},
	)
}

// localEchoServiceClient adapts a EchoServiceServer to the
// EchoServiceClient interface by dispatching each call directly to the
// server impl. Use it to wire MCP tools against your in-process gRPC service
// without a network round trip.
type localEchoServiceClient struct{ srv EchoServiceServer }

// NewLocalEchoServiceClient returns a EchoServiceClient that
// dispatches every call directly to srv. Pair it with RegisterEchoServiceTools
// when the MCP server runs in the same process as your gRPC service.
func NewLocalEchoServiceClient(srv EchoServiceServer) EchoServiceClient {
	return &localEchoServiceClient{srv: srv}
}

func (c *localEchoServiceClient) Echo(ctx context.Context, in *EchoRequest, _ ...grpc.CallOption) (*, error) {
	return c.srv.Echo(ctx, in)
}

func (c *localEchoServiceClient) Reverse(ctx context.Context, in *ReverseRequest, _ ...grpc.CallOption) (*, error) {
	return c.srv.Reverse(ctx, in)
}

